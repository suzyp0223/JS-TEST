const fs = require("fs");
const input = readFileSync("/dev/stdin").toString().split(" ");

solution(input);

function solution(input) {
  let answer = 0;
  /* 코드 작성 */
  console.log(answer);
}