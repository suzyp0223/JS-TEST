

// arr 정수배열
//이중 0에서 가장 가까운 값을 구하는 프로그램을 구현.
// 0 < arr.length <= 100
// 0 <= arr[i] <= 100
const solution = (arr) => {

  var answer = arr.sort().join('');
   Number(answer[0])

 
}


console.log(solution([5, 2, 1, 3])); // 1

